apiVersion: v2
name: homelab-policies
description: |
  Alles, was eine CRD voraussetzt und deshalb zuletzt kommt:

    - die Kyverno-Regeln, allen voran die auf ingressClassName: public
    - die beiden Router von ingress-public zur CrowdSec-LAPI und zu step-ca
    - die ClusterIssuer von cert-manager für die internen Namen
type: application
version: 0.1.0
appVersion: "1.0"
