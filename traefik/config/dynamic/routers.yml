# yaml-language-server: $schema=https://json.schemastore.org/traefik-v3-dynamic.json
# Traefik v3 Dynamic Configuration - Routers
# Documentation: https://doc.traefik.io/traefik/v3.0/routing/routers/

# Router werden in der Regel automatisch über Docker Labels definiert
# Beispiel siehe traefik/docker-compose.yml

# Statische Router können hier definiert werden, falls nicht Docker Provider genutzt wird
#http:
#  routers:
#    my-router:
#      rule: "Host(`example.com`)"
#      service: my-service
#      entryPoints:
#        - websecure
#      tls:
#        certResolver: letsencrypt
#      middlewares:
#        - secure-headers@file

# Services können hier definiert werden, wenn nicht Docker Provider genutzt wird
# http:
#   services:
#     my-service:
#       loadBalancer:
#         servers:
#           - url: "http://private-ip-server-1/"

