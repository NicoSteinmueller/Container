# yaml-language-server: $schema=https://json.schemastore.org/traefik-v3-dynamic.json
# Traefik v3 Dynamic Configuration - Routers
# Documentation: https://doc.traefik.io/traefik/v3.0/routing/routers/

# Nur als Beispiel - Router werden in der Regel automatisch über Docker Labels definiert
#http:
#  routers:
    # Beispiel-Router für Traefik Dashboard
    # dashboard:
    #   rule: "Host(`traefik.example.com`)"
    #   service: api@internal
    #   entryPoints:
    #     - websecure
    #   tls:
    #     certResolver: letsencrypt
    #   middlewares:
    #     - secure-headers
    #     # - basic-auth  # Optional: Dashboard mit Auth schützen
 - ping

# Services können hier definiert werden, wenn nicht Docker Provider genutzt wird
# http:
#   services:
#     my-service:
#       loadBalancer:
#         servers:
#           - url: "http://private-ip-server-1/"

