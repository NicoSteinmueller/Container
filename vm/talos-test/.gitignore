# Aus Terraform-Outputs erzeugte Zugangsdaten - enthalten Client-Zertifikate
# mit vollem Cluster-Zugriff und gehören nicht ins Repo.
talosconfig
kubeconfig

# Enthält die eigene Netz- und Zugangskonfiguration - gehört nicht ins Repo.
terraform.tfvars
